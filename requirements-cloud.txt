# Cloud deployment requirements — ADE20K SegFormer via transformers.
# NOTE: torch + transformers are heavy; prefer the SegFormer-B0 checkpoint
# (SEGFORMER_MODEL_ID=nvidia/segformer-b0-finetuned-ade-512-512) and a host
# with enough RAM. The free 512MB tier is not sufficient for this stack.
flask>=3.0
numpy>=1.26
opencv-python-headless>=4.9
torch>=2.1
transformers>=4.42
pillow>=10.0
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
pyyaml>=6.0
httpx>=0.27
gunicorn>=22.0
