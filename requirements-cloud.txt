# Cloud deployment — ONNX-only inference (no torch/transformers).
# The INT8 model (segformer_b0_ade20k_int8.onnx) must be in the repo.
# Smaller image = faster cold starts on Render/Railway.
flask>=3.0
numpy>=1.26
opencv-python-headless>=4.9
onnxruntime>=1.18
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
pyyaml>=6.0
httpx>=0.27
gunicorn>=22.0
