# Cloud deployment requirements — uses ONNX runtime instead of PyTorch
# so the server stays under 512MB RAM on free-tier hosting.
flask>=3.0
numpy>=1.26
opencv-python-headless>=4.9
onnxruntime>=1.18
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
pyyaml>=6.0
httpx>=0.27
gunicorn>=22.0
